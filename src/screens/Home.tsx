// P0 placeholder. The balance region, Glance / Play-my-week buttons and the
// week list arrive in P1 (SPEC 10). No h1 here — SPEC 10 puts the single h1 in
// the shared Header, which is a P1 component.
export default function Home() {
  return <p>Home</p>;
}
